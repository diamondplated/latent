"""Vendored model architectures.

These exist so the conversion scripts don't depend on BasicSR, which is
unmaintained, unimportable on any modern torch, and carries an unpatched
advisory of its own. See rrdbnet.py for the full reasoning.
"""
